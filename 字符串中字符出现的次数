#include<stdio.h>
int main()
{
	char a[] = "I love china so much";
	int i;
	int sz = sizeof(a) / sizeof(a[0]);
	int count=0;
	for (i = 1; i <= sz; i++)
	{
		if ('h' == a[i])
			count++;
	}
	printf("%d", count);
	return 0;
}
